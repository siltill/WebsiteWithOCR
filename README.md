📝 WebsiteWithOCR

Полноценное веб-приложение для распознавания текста (OCR) с использованием Tesseract OCR, Poppler, Python и Go.
Фронтенд написан на Vue (Vite), бэкенд — на Go, OCR-ядро — на Python.

Приложение полностью упаковано в Docker, что позволяет запускать его на любом устройстве без установки Python, Go, Node.js, Poppler, Tesseract и других зависимостей.

🚀 Возможности

📤 Загрузка изображений и PDF-файлов

🔍 Обработка OCR через Python (Tesseract + Poppler)

⚙️ Go-сервер обрабатывает запросы /api/upload и вызывает Python-скрипты

🌐 Vue-фронтенд предоставляет удобный веб-интерфейс

🐳 Полная контейнеризация: backend + frontend в отдельных Docker-образах

📦 Готов к деплою на любом устройстве

🧱 Архитектура проекта
project-root/
 ├── backend/              # Go-сервер
 │     └── main.go
 ├── python_core/          # OCR ядро на Python
 │     ├── main.py
 │     ├── ocr.py
 │     ├── preprocessing.py
 │     └── requirements.txt
 ├── frontend/
 │     └── ocr_app_frontend/  # Vue-фронтенд
 │             ├── src/
 │             ├── public/
 │             ├── package.json
 │             └── vite.config.js
 ├── backend.Dockerfile
 ├── frontend.Dockerfile
 ├── docker-compose.yml
 └── README.md

🐳 Запуск через Docker (рекомендуется)
1. Установить Docker Desktop

https://www.docker.com/products/docker-desktop/

2. Запуск приложения

Выполните в корне проекта:

docker compose up --build


Откройте браузер:

Frontend: http://localhost:5173

Backend API: http://localhost:8080/api/upload
 (POST запросы)

🧪 API
POST /api/upload

Загрузка изображения или PDF для OCR.

Параметры формы:
Поле	Тип	Описание
file	multipart/form-data	Изображение или PDF
Пример ответа:
{
  "text": "Пример распознанного текста",
  "average_confidence": 87.4,
  "processing_time_ms": 312,
  "error": ""
}

🧰 Запуск локально (без Docker)

Использовать только если вам нужно разработать или дебажить код.

📌 1. Установка Python OCR ядра

Установить зависимости:

pip install -r python_core/requirements.txt


Установить Tesseract OCR:

Windows:
https://github.com/UB-Mannheim/tesseract/wiki

Poppler для PDF:
https://blog.alivate.com.au/poppler-windows/

📌 2. Запуск Go-сервера
cd backend
go run main.go


Сервер будет доступен по адресу:

http://localhost:8080

📌 3. Запуск фронтенда
cd frontend/ocr_app_frontend
npm install
npm run dev


Открыть:

http://localhost:5173

🔧 Как передать приложение на другое устройство

Есть два способа.

✔ 1. Перенести проект и пересобрать Docker

На новом устройстве:

git clone https://github.com/<username>/WebsiteWithOCR.git
cd WebsiteWithOCR
docker compose up --build

✔ 2. Перенести готовые Docker-образы (без сборки)

Экспорт:

docker save -o backend.tar ocrapp-backend
docker save -o frontend.tar ocrapp-frontend


Импорт на другом ПК:

docker load -i backend.tar
docker load -i frontend.tar
docker compose up


Этот способ самый быстрый для сдачи курсовой.

🛠 Используемые технологии
Backend:

Go 1.22

net/http

exec для вызова Python

OCR ядро:

Python 3

Tesseract OCR

Poppler

Pillow, PyTesseract и др.

Frontend:

Vue 3

Vite

Tailwind (если используется)

Fetch API

DevOps:

Docker

Docker Compose

Nginx (для фронта)

👨‍🎓 Автор проекта

Кириллов В.М.
GitHub: https://github.com/siltill

Проект подготовлен для курсовой работы по разработке программного обеспечения.
